{"context specification":{
"query":"g.V().has('eventType').as('y').values('eventType').as('type').select('y').out().has('subjectType','SUBJECT_PROCESS').as('x').values('uuid').as('id').select('x').out().has('path').values('path').as('filepath').select('id','type','filepath').dedup()",
"objects":"id",
"attributes":"type,filepath"
}
}

