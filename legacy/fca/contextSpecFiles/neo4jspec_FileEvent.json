{"context specification":{
"query":"g.V().has('eventType').as('y').values('eventType').as('type').select('y').out().has('subjectType').as('x').select('x').out().has('path').values('path').as('filepath').select('filepath','type').dedup()",
"objects":"filepath",
"attributes":"type"
}
}
