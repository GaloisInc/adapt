{"context specification":{
"query":"g.V().hasLabel('AdmSubject').has('subjectType','SUBJECT_PROCESS').as('x').values('uuid').as('id').select('x').out().hasLabel('AdmPathNode').has('path').as('y').values('path').as('filepath').select('id','filepath').dedup()",
"objects":"id",
"attributes":"filepath"
 }
}
