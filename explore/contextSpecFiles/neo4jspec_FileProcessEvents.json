{"context specification":{
"query":"g.V().has('subjectType','SUBJECT_PROCESS').as('x').select('x').in().has('eventType').as('y').values('eventType').as('type').select('x').values('uuid').as('id').select('x').out().has('path').as('y').values('path').as('filepath').select('filepath','id','type').dedup()",
"objects":"filepath",
"attributes":"id,type"
}
}
